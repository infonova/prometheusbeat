################### Prometheusbeat Configuration Example #########################

############################# Prometheusbeat ######################################

prometheusbeat:
  # Listen port of the server. Defaults to :8080
  listen: ":8080"
  # Context path. Defaults to /prometheus
  context: "/prometheus"
